{
  "name": "crypto-locker",
  "version": "1.0.0",
  "description": "Time-lock crypto wallet smart contract (deposit ETH/tokens with lock-in time)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
